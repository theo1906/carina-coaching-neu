'use client';

// This component is kept for future use when multiple languages are needed
// Currently, it's hidden as we're only supporting German

export default function LanguageSwitcher() {
  return null; // Hide the language switcher as we only have German
}
